# Commercial access security

The central SQL action matrix consumes the established entitlement resolver. It does not reinterpret plans or consult browser intent, provider return parameters or feature keys. Resolution uses the transaction timestamp, fixed search paths and static SQL, without mutating subscription state. The migration creates no second commercial truth.

`get_my_commercial_access_summary` requires a canonical PT identity. `get_workspace_commercial_access` requires an owner or authorized active member. `get_client_coaching_access` requires the linked client or authorized staff. Workspace-owner identity comes from the database; client/assignment ancestry determines coached writes. The owner and client/workspace response schemas use strict Zod validation, exact modes/actions, UUIDs, timestamps and consistency rules. Invalid responses fail closed with a retry state.

Private resolver/assertion/row-scope/trigger functions have explicit execute revocations. Only audience-safe entry points are granted to runtime roles. Role authorization is not replaced: original RLS, column privileges, RPC checks and assigned-client restrictions still apply. A full commercial contract cannot grant a domain permission. Before a denial discloses an owner mode, the assertion checks whether the actor is that owner or an authorized workspace/client audience. Public/prospect denials carry only the stable public code.

## Mutation protection

The [direct-write inventory](commercial-access-write-inventory.json) records literal application table writes. A contract test fails if a newly discovered literal write lacks a commercial trigger or the explicit personal-notification exception. Dynamic table use in `supabase-safe.ts` is read-only; IndexedDB drafts are local personal state. Storage uses additional restrictive INSERT/UPDATE/DELETE policies for coached photos and workspace branding. Existing storage authorization and historical read policies remain in place. Personal profile media does not itself publish a profile; publication remains guarded.

Narrow before-row guards cover delivery and template parents/children, conversations/messages/typing, clients and team mutations, workspace/business settings, packages, public profiles/leads and assignments. Ancestry is resolved with static queries, including workout sessions through their assigned workout before legacy normalization fills in `client_id`. Both old and new scopes are checked on moves. Capacity triggers remain authoritative after the commercial boundary.

Explicit reducing transitions have a field allowlist; attaching unrelated business changes does not make them remediation. Parent deletion is guarded before dependent FK cascades. A nested cascading delete with an already-removed parent can finish cleanup; top-level writes with missing scope fail closed. No unqualified delivery DELETE exemption exists. Private drafts and security/preferences remain separate from public business configuration.

Database administrator and service-role writes retain their existing trusted privileges for migrations, provider reconciliation and service-owned records. Browser runtime roles cannot set the privileged role or replication setting. Existing user-facing definer RPCs retain original authentication and authorization and execute the commercial table guards under their authenticated request role. Public application preflight is also explicit before rate-limit/lead mutation. Platform marketing lead submission is acquisition for RepSync itself, not a coach account; wearable ingestion is independent health-data service work, and exercise search is read-only.

Read helpers that previously created check-ins or conversations now avoid materialization when interaction is unavailable, while returning existing history. No access-resolution function materializes product data.

## Privacy and errors

Exact codes: `ACCOUNT_ACCESS_ACTION_NOT_ALLOWED`, `ACCOUNT_ACCESS_EXISTING_DELIVERY_ONLY`, `ACCOUNT_ACCESS_READ_ONLY`, `ACCOUNT_ACCESS_EXPIRED`, `ACCOUNT_ACCESS_ONBOARDING_ONLY`, `ACCOUNT_ACCESS_OWNER_RECOVERY_REQUIRED`, `WORKSPACE_COMMERCIAL_ACCESS_UNAVAILABLE`, `CLIENT_COACHING_INTERACTION_UNAVAILABLE`, and `PUBLIC_COACH_NOT_ACCEPTING_APPLICATIONS`.

Owner denials may include mode, effective date and recovery path/action. Team denials give generic contact-owner guidance. Client/public denials omit plan, price, capacity, billing account, payment failure and provider state. Unrelated permission/provider failures are not reclassified. UI denial paths preserve form/draft state. Access API telemetry includes only safe code, domain and audience tags; it never sends record identifiers, payment details, content or raw SQL/provider errors. Database denials that roll back are not durable audit history. No email or push delivery is added.

## Migration and rollback

One forward transaction adds functions, guards and policies. It does not update subscription history, catalogue prices/capacities, trial dates, provider mappings, publication preferences or domain rows. Historical migrations remain unchanged. Rollback requires an application revert and a reviewed compensating forward migration restoring affected functions/policies/guards together. Preserve all subscription, provider, plan-change, capacity and domain history. No remote operation is part of this implementation.

## PR-PRICE-08 correction audit (2026-09-12)

The unmerged PR-PRICE-08 migration is corrected in place. Invitation eligibility is shared by capacity and remediation through the private `account_capacity_pending_seat_invites(owner, at)` helper. It is not executable by runtime roles. The owner-only remediation RPC emits one deterministic representative per canonical identity; revoking one duplicate leaves any remaining current commitment visible on refresh.

Conversation discovery and linked-client read authorization no longer inspect compatibility `clients.status`. `get_client_coaching_access` uses `relationship_status = active` and the explicit lifecycle allowlist invited/onboarding/active/paused; completed/churned/unknown/null are historical only. No unknown value is defaulted to active. A linked client can read their own historical conversation, including removed/transferred-out records; this does not grant another client or coach access. The message/conversation/typing mutation guard checks the same interaction resolver after ordinary commercial authorization, so historical read access cannot grant writes. Staff permissions remain independent. The existing unique constraint handles concurrent materialization retries. Lead conversation authorization is unchanged and tested independently.

### Storage mutation audit

The original restrictive INSERT/UPDATE policies missed DELETE. The corrected restrictive DELETE policy calls the same scope helper and intersects existing permissive role/domain policies. No ordinary object deletion is classified as remediation. Baseline and unsubmitted check-in photos use delivery_write; client-owned medical files remain independent self-service. Workspace branding explicitly uses business_configuration_write. Existing branding policy permits INSERT/DELETE but deliberately supplies no UPDATE permission. The full/grace/restricted/expired SQL matrix performs real INSERT, UPDATE and DELETE operations for all four buckets, checks affected row counts, and verifies unauthorized cross-client writes remain denied. Storage's own `storage.allow_delete_query` setting is used only in transactional SQL tests to emulate API deletion; it does not bypass RLS.

Application mutation callers inspected: baseline photo uploads, client check-in uploads, client and coach medical upload/removal, workspace branding upload/removal, and personal profile media upload. Download-only helpers remain read-only. No service-role storage mutation helper exists. Existing explicit domain-remediation deletion and guarded FK cleanup remain separate from storage API removal.

### Trusted service-role inventory

Repository search covered all Edge Functions, shared handler/runtime modules and server-side scripts, including RPC indirection. There are **zero user-facing service-role writes to tables protected by the PR-PRICE-08 commercial domain trigger**. The [literal inventory](commercial-access-write-inventory.json) now includes all Edge Function direct writes as well as browser writes. A permanent unit inventory gate rejects unreviewed service table mutations and fails if an independent exception becomes commercially guarded.

| Entry point                                                                                                                      | Authentication and real scope                                                                                                     | Writes / commercial classification                                                                            | Independent authorization and bypass review                                                                                               |
| -------------------------------------------------------------------------------------------------------------------------------- | --------------------------------------------------------------------------------------------------------------------------------- | ------------------------------------------------------------------------------------------------------------- | ----------------------------------------------------------------------------------------------------------------------------------------- |
| billing-create-lemon-squeezy-checkout                                                                                            | Server `auth.getUser(token)`; owner-scoped begin RPC uses caller JWT                                                              | Billing checkout attempts/customer reconciliation only; billing_manage remains available in every mode        | Server-resolved attempt and owner, private service RPCs; no caller trusted/admin option                                                   |
| billing-create-customer-portal-link                                                                                              | Server token validation; `p_owner = user.id`                                                                                      | No coached-domain write; billing recovery                                                                     | Database subscription ownership rechecked; provider response validation; no caller bypass                                                 |
| billing-preview-plan-change, billing-change-subscription-plan, billing-refresh-plan-change, billing-cancel-scheduled-plan-change | Server token validation; `p_owner = owner.id`                                                                                     | Private billing operation/subscription history RPCs; billing_manage                                           | Database account/operation ownership, target/version and provider checks; no caller bypass                                                |
| billing-lemon-squeezy-webhook                                                                                                    | Provider signature, environment/store and delivery identity                                                                       | Trusted provider reconciliation of billing records                                                            | Maintenance/service boundary; no browser identity or coached-domain mutation                                                              |
| open-wearables                                                                                                                   | `auth.getUser(bearerToken)`; clients query binds both requested id and authenticated user; settings use the returned workspace_id | Connection, daily metrics, sleep, health score, activity and sync-run tables; independent health self-service | Provider/action/redirect and workspace integration checks; request cannot supply service-role authorization or a different resolved owner |
| marketing-lead-submit                                                                                                            | Intentionally public platform form; validated payload and spam checks                                                             | marketing_leads, not coach pt_hub_leads; platform acquisition                                                 | No owner/workspace commercial scope; no trusted/admin option                                                                              |
| exercise-dataset-search                                                                                                          | Server token validation and database PT/membership checks                                                                         | Read-only; no commercial mutation assertion needed                                                            | Existing provider access rules; no caller bypass                                                                                          |
| scripts/seed-local-pt-demo.mjs, scripts/render-client-experience-media.mjs                                                       | Operator-run fixture/render tooling, not HTTP handlers                                                                            | Trusted local fixture work                                                                                    | Excluded from user-facing mutation paths; neither invoked here                                                                            |

Because none of these service mutations target guarded coached-domain tables, there is no missing coached commercial assertion to add. A future service handler that writes one must authenticate, derive scope from the database, assert the action and separately enforce domain permissions; service_role cannot rely on the browser-trigger path. Tests here use mocked provider dependencies only. No real provider or notification endpoint was called.
